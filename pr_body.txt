- [x] 既存 ID（`#landing`, `#archive-section`, `#paper-view`, `#paper-grid`, `#search`, `#sort-select`, `#evo-log-view`, `#evo-log-list`, `#footer-gen`, `#footer-papers`, `#footer-reviewed`, `#header-gen`, `#evo-back-nav`, `#footer-evo-link`, `#header-evo-link`）が無傷
- [x] 既存 CSS クラス（`.verdict-badge`, `.persona-row`, `.evo-entry`, `.meta-badge`, `.ref-link`, `.paper-card`, `.stat`, `.badge`）の振る舞いを破壊していない
- [x] 既存関数のシグネチャを変えていない
- [x] CSS 変数のみ使用（生のカラーコード追加なし）
- [x] 絵文字を追加していない
- [x] 外部 `<script src>` / `<link href>` を新規追加していない（CI が物理ブロック）
- [x] アニメーションは最小限（既存の `pulse` / `t-line` / `writeSlide` 程度の控えめさ）
- [x] データが空（reviews 0 件、generations 0 件）の状態でも壊れない

## 改善理由
既にキーボードナビゲーション機能（`/`で検索ボックスにフォーカス）が実装されていましたが、ユーザーにその存在を知らせるヒントがUI上にありませんでした。そのため、プロンプトの推奨改善案に沿って、検索ボックスのプレースホルダーに `[ / ]` というヒントを控えめに追加しました。

## 根拠データ
- アクセシビリティとユーザビリティの向上（データ件数に依存しない全般的な改善）

## 差分行数
- `docs/index.html` : 1行変更
