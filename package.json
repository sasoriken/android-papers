{
  "name": "android-papers",
  "version": "1.0.0",
  "type": "module",
  "description": "UNIT-Ω Architecture Archive — automated paper generation via Claude API",
  "scripts": {
    "generate": "node scripts/generate.js",
    "generate:batch": "node scripts/generate.js --count 8",
    "validate": "node scripts/validate.js --all",
    "dev": "npx serve site -l 3000"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.29.0"
  },
  "engines": {
    "node": ">=20"
  }
}
