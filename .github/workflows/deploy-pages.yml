# このワークフローは廃止されました。
# GitHub Pages は docs/ フォルダからの静的デプロイに移行しました。
# Settings > Pages > Source: "Deploy from a branch" / main / /docs を設定してください。
name: "[DEPRECATED] Deploy to GitHub Pages"
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Pages now deploys from docs/ folder."
